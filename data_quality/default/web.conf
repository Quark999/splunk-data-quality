# web.conf — app-scoped Splunk Web settings for data_quality
# safe_mode = false allows outputlookup in dashboard searches without
# the "potential security risk" prompt. Scoped to this app only.

[settings]
safe_mode = false
